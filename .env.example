# Optional MQTT configuration for the Python weather/legacy traffic helpers.
# Copy values into your shell or a local untracked .env loader. Unity's current
# C# bridge is configured through the Inspector or deployment configuration.
FAA_MQTT_HOST=127.0.0.1
FAA_MQTT_PORT=1883
FAA_MQTT_USERNAME=
FAA_MQTT_PASSWORD=
